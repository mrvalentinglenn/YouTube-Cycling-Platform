# The Supabase secret key must never appear in this directory, in any
# form. It bypasses Row Level Security and belongs only to the collection
# job, which reads it from the root .env locally and from GitHub Secrets
# in Actions.

VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=